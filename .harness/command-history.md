# Command History

This file records harness-relevant commands for session recovery. It is not a full shell history.

## 2026-05-11
- `pwd`
- `Get-ChildItem -Force`
- `Get-ChildItem -Recurse -File -Force | ...`
- `git status --short --branch`
- `Get-ChildItem -Name package.json,pyproject.toml,Cargo.toml,go.mod,Makefile -ErrorAction SilentlyContinue`
- `Get-Content -Raw AGENTS.md`
- `Get-Content -Raw pyproject.toml`
- `Get-Content -Raw README.md`
- `Get-Content -Raw .gitignore`
- `Get-Command bash,sh -ErrorAction SilentlyContinue`
- `uv run python -m pytest -q`
- `uv run python -m json.tool .harness/session-state.json > $null`
- PowerShell required-file check equivalent to `scripts/harness_check.sh`
- `git status --short --branch`
